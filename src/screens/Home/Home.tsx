import {View} from 'react-native';
import React from 'react';
import {globalStyles} from '../../styles';

export const Home = () => {
  return <View style={globalStyles.fullScreenContainerWhite} />;
};
